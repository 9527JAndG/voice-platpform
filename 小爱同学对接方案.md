# 小爱同学智能音箱对接方案 - 扫地机器人控制

## 📖 方案概述

小爱同学是小米推出的智能语音助手，基于小米 IoT 开放平台。与天猫精灵和小度音箱类似，也采用 OAuth 2.0 授权 + Webhook 的方式进行智能家居设备对接。

## 🔄 三平台对比分析

| 对比项 | 天猫精灵 | 小度音箱 | 小爱同学 |
|--------|---------|---------|---------|
| 开发平台 | 天猫精灵开放平台 | 百度 DuerOS | 小米 IoT 开放平台 |
| 授权协议 | OAuth 2.0 | OAuth 2.0 | OAuth 2.0 |
| 通信方式 | HTTPS + JSON | HTTPS + JSON | HTTPS + JSON |
| Namespace | AliGenie.Iot.* | DuerOS.ConnectedHome.* | MiAI.SmartHome.* |
| 设备类型 | robot_cleaner | ROBOT_CLEANER | vacuum-cleaner |
| 请求格式 | 扁平结构 | 嵌套结构 | 简化结构 |
| 市场份额 | ~30% | ~25% | ~20% |
| 开发难度 | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |

## 🎯 核心流程

### 1. OAuth 授权流程（与其他平台相同）

```
用户 → 小爱 App → 授权请求
                    ↓
              /authorize (展示授权页面)
                    ↓
              用户确认授权
                    ↓
              生成 code 并重定向
                    ↓
              /token (code换token)
                    ↓
              返回 access_token
```

### 2. 设备发现流程

```
用户: "小爱同学，发现设备"
         │
         ▼
    小米 IoT 平台
         │
         ▼
    POST /miai/discovery
    Header: Authorization: Bearer {token}
         │
         ▼
    验证 Token → 查询用户设备 → 返回设备列表
```

### 3. 设备控制流程

```
用户: "小爱同学，打开扫地机器人"
         │
         ▼
    小米 IoT 平台 (解析语音指令)
         │
         ▼
    POST /miai/control
    {
      "intent": "turn-on",
      "device_id": "robot_001"
    }
         │
         ▼
    验证 Token → 执行设备操作 → 返回结果
```

## 📋 开发步骤

### 步骤 1: 准备工作

#### 1.1 注册开发者账号
- 访问小米 IoT 开放平台
- 注册并认证开发者账号
- 创建智能家居技能

#### 1.2 环境准备
```bash
- JDK 21+
- MySQL 8.0+
- Maven 3.6+
- 公网 HTTPS 地址（生产环境必须）
```

### 步骤 2: 复用现有项目架构

#### 2.1 保持不变的部分（100% 复用）
- ✅ OAuth 2.0 授权流程
- ✅ 数据库设计
- ✅ Service 层业务逻辑
- ✅ Repository 层
- ✅ 授权页面

#### 2.2 需要调整的部分
- 🔄 请求/响应 DTO 格式
- 🔄 Controller 层接口适配
- 🔄 设备类型和属性映射

### 步骤 3: 创建小爱专用 DTO

#### 3.1 MiAI 请求对象

```java
package com.example.aligenie.dto;

import lombok.Data;
import java.util.Map;

/**
 * 小爱同学请求对象
 */
@Data
public class MiAIRequest {
    
    private String intent;              // 意图：turn-on, turn-off, pause
    private String deviceId;            // 设备ID
    private Map<String, Object> params; // 额外参数
    private String requestId;           // 请求ID
    
    // 小爱的请求格式更简洁
}
```

#### 3.2 MiAI 响应对象

```java
package com.example.aligenie.dto;

import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.NoArgsConstructor;
import java.util.*;

/**
 * 小爱同学响应对象
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
public class MiAIResponse {
    
    private Integer code;               // 状态码：0-成功，其他-失败
    private String message;             // 消息
    private Map<String, Object> data;   // 数据
    
    /**
     * 创建成功响应
     */
    public static MiAIResponse success() {
        return new MiAIResponse(0, "success", new HashMap<>());
    }
    
    /**
     * 创建成功响应（带数据）
     */
    public static MiAIResponse success(Map<String, Object> data) {
        return new MiAIResponse(0, "success", data);
    }
    
    /**
     * 创建错误响应
     */
    public static MiAIResponse error(Integer code, String message) {
        return new MiAIResponse(code, message, new HashMap<>());
    }
    
    /**
     * 创建设备发现响应
     */
    public static MiAIResponse discoveryResponse(List<Map<String, Object>> devices) {
        Map<String, Object> data = new HashMap<>();
        data.put("devices", devices);
        return success(data);
    }
}
```

### 步骤 4: 创建小爱专用 Controller

```java
package com.example.aligenie.controller;

import dto.com.voice.platform.MiAIRequest;
import dto.com.voice.platform.MiAIResponse;
import model.com.voice.platform.Device;
import repository.com.voice.platform.DeviceRepository;
import service.com.voice.platform.OAuthService;
import lombok.extern.slf4j.Slf4j;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import java.util.*;

/**
 * 小爱同学智能家居控制器
 */
@Slf4j
@RestController
@RequestMapping("/miai")
public class MiAIController {

    @Autowired
    private OAuthService oauthService;

    @Autowired
    private DeviceRepository deviceRepository;

    /**
     * 设备发现接口
     * POST /miai/discovery
     */
    @PostMapping("/discovery")
    public ResponseEntity<?> discovery(
            @RequestHeader("Authorization") String authorization) {

        log.info("小爱设备发现请求");

        // 验证 Token
        String accessToken = extractToken(authorization);
        if (!oauthService.validateAccessToken(accessToken)) {
            return ResponseEntity.status(HttpStatus.UNAUTHORIZED)
                    .body(MiAIResponse.error(401, "访问令牌无效或已过期"));
        }

        // 获取用户ID
        Long userId = 1L;

        // 查询用户设备
        List<Device> devices = deviceRepository.findByUserId(userId);

        // 构建设备列表
        List<Map<String, Object>> deviceList = new ArrayList<>();

        for (Device device : devices) {
            Map<String, Object> deviceInfo = new HashMap<>();
            deviceInfo.put("device_id", device.getDeviceId());
            deviceInfo.put("device_name", device.getDeviceName());
            deviceInfo.put("device_type", "vacuum-cleaner"); // 小爱的设备类型
            deviceInfo.put("online", "online".equals(device.getStatus()));

            // 设备属性
            Map<String, Object> properties = new HashMap<>();
            properties.put("power_state", device.getPowerState());
            properties.put("work_mode", device.getWorkMode());
            properties.put("battery_level", device.getBatteryLevel());
            deviceInfo.put("properties", properties);

            // 支持的操作
            List<String> actions = Arrays.asList(
                    "turn-on", "turn-off", "pause", "continue", "set-mode"
            );
            deviceInfo.put("actions", actions);

            deviceList.add(deviceInfo);
        }

        log.info("小爱设备发现完成: userId={}, deviceCount={}", userId, deviceList.size());

        return ResponseEntity.ok(MiAIResponse.discoveryResponse(deviceList));
    }

    /**
     * 设备控制接口
     * POST /miai/control
     */
    @PostMapping("/control")
    public ResponseEntity<?> control(
            @RequestHeader("Authorization") String authorization,
            @RequestBody MiAIRequest request) {

        String intent = request.getIntent();
        String deviceId = request.getDeviceId();

        log.info("小爱设备控制请求: intent={}, deviceId={}", intent, deviceId);

        // 验证 Token
        String accessToken = extractToken(authorization);
        if (!oauthService.validateAccessToken(accessToken)) {
            return ResponseEntity.status(HttpStatus.UNAUTHORIZED)
                    .body(MiAIResponse.error(401, "访问令牌无效或已过期"));
        }

        // 查询设备
        Optional<Device> deviceOpt = deviceRepository.findByDeviceId(deviceId);
        if (!deviceOpt.isPresent()) {
            return ResponseEntity.ok(MiAIResponse.error(404, "设备不存在"));
        }

        Device device = deviceOpt.get();

        // 执行控制操作
        MiAIResponse response;

        switch (intent) {
            case "turn-on":
                device.setPowerState("on");
                deviceRepository.save(device);
                response = MiAIResponse.success();
                log.info("小爱设备开机: deviceId={}, deviceName={}", deviceId, device.getDeviceName());
                break;

            case "turn-off":
                device.setPowerState("off");
                deviceRepository.save(device);
                response = MiAIResponse.success();
                log.info("小爱设备关机: deviceId={}, deviceName={}", deviceId, device.getDeviceName());
                break;

            case "pause":
                response = MiAIResponse.success();
                log.info("小爱设备暂停: deviceId={}", deviceId);
                break;

            case "continue":
                response = MiAIResponse.success();
                log.info("小爱设备继续: deviceId={}", deviceId);
                break;

            case "set-mode":
                // 从 params 中获取模式
                if (request.getParams() != null && request.getParams().containsKey("mode")) {
                    String mode = request.getParams().get("mode").toString();
                    device.setWorkMode(mode);
                    deviceRepository.save(device);
                    log.info("小爱设置模式: deviceId={}, mode={}", deviceId, mode);
                }
                response = MiAIResponse.success();
                break;

            default:
                response = MiAIResponse.error(400, "不支持的操作: " + intent);
                log.warn("小爱不支持的操作: intent={}", intent);
        }

        return ResponseEntity.ok(response);
    }

    /**
     * 设备查询接口
     * POST /miai/query
     */
    @PostMapping("/query")
    public ResponseEntity<?> query(
            @RequestHeader("Authorization") String authorization,
            @RequestBody MiAIRequest request) {

        String deviceId = request.getDeviceId();

        log.info("小爱设备查询请求: deviceId={}", deviceId);

        // 验证 Token
        String accessToken = extractToken(authorization);
        if (!oauthService.validateAccessToken(accessToken)) {
            return ResponseEntity.status(HttpStatus.UNAUTHORIZED)
                    .body(MiAIResponse.error(401, "访问令牌无效或已过期"));
        }

        // 查询设备
        Optional<Device> deviceOpt = deviceRepository.findByDeviceId(deviceId);
        if (!deviceOpt.isPresent()) {
            return ResponseEntity.ok(MiAIResponse.error(404, "设备不存在"));
        }

        Device device = deviceOpt.get();

        // 构建设备状态
        Map<String, Object> data = new HashMap<>();
        data.put("power_state", device.getPowerState());
        data.put("work_mode", device.getWorkMode());
        data.put("battery_level", device.getBatteryLevel());
        data.put("status", device.getStatus());

        log.info("小爱设备查询完成: deviceId={}", deviceId);

        return ResponseEntity.ok(MiAIResponse.success(data));
    }

    /**
     * 从 Authorization header 中提取 token
     */
    private String extractToken(String authorization) {
        if (authorization != null && authorization.startsWith("Bearer ")) {
            return authorization.substring(7);
        }
        return null;
    }
}
```

### 步骤 5: 数据库配置

在现有数据库中添加小爱客户端：

```sql
INSERT INTO oauth_clients (client_id, client_secret, redirect_uri, user_id) 
VALUES ('miai_client_id', 'miai_client_secret', 'https://xiaoai.mi.com/callback', 1)
ON DUPLICATE KEY UPDATE client_secret='miai_client_secret';
```

### 步骤 6: 配置小米 IoT 开放平台

#### 6.1 创建技能
1. 登录小米 IoT 开放平台
2. 创建"智能家居"技能
3. 填写技能基本信息

#### 6.2 配置 OAuth
```
授权地址: https://your-domain.com/authorize
Token 地址: https://your-domain.com/token
Client ID: miai_client_id
Client Secret: miai_client_secret
```

#### 6.3 配置 Webhook
```
设备发现接口: https://your-domain.com/miai/discovery
设备控制接口: https://your-domain.com/miai/control
设备查询接口: https://your-domain.com/miai/query
```

### 步骤 7: 测试流程

#### 7.1 OAuth 授权测试
```bash
curl "http://localhost:8080/authorize?client_id=miai_client_id&redirect_uri=https://xiaoai.mi.com/callback&state=xyz&response_type=code"
```

#### 7.2 获取 Token
```bash
curl -X POST "http://localhost:8080/token" \
  -d "grant_type=authorization_code" \
  -d "client_id=miai_client_id" \
  -d "client_secret=miai_client_secret" \
  -d "code=YOUR_CODE"
```

#### 7.3 设备发现测试
```bash
curl -X POST "http://localhost:8080/miai/discovery" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -H "Content-Type: application/json"
```

#### 7.4 设备控制测试
```bash
curl -X POST "http://localhost:8080/miai/control" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "intent": "turn-on",
    "deviceId": "robot_001",
    "requestId": "test-001"
  }'
```

## 🎮 语音控制示例

配置完成后，可以使用以下语音命令：

- "小爱同学，打开扫地机器人"
- "小爱同学，关闭扫地机器人"
- "小爱同学，暂停扫地机器人"
- "小爱同学，继续扫地"
- "小爱同学，扫地机器人切换到自动模式"
- "小爱同学，扫地机器人电量还有多少"

## 📊 请求响应格式对比

### 天猫精灵格式
```json
{
  "header": {"namespace": "AliGenie.Iot.Device.Control", "name": "TurnOn"},
  "payload": {"deviceId": "robot_001"}
}
```

### 小度音箱格式
```json
{
  "header": {"namespace": "DuerOS.ConnectedHome.Control", "name": "TurnOnRequest"},
  "payload": {"appliance": {"applianceId": "robot_001"}}
}
```

### 小爱同学格式（最简洁）
```json
{
  "intent": "turn-on",
  "deviceId": "robot_001",
  "requestId": "test-001"
}
```

## 🔧 关键差异点

### 1. 请求格式
- **天猫精灵/小度**: 使用 header + payload 结构
- **小爱同学**: 使用扁平的 JSON 结构，更简洁

### 2. 操作命名
- **天猫精灵**: TurnOn, TurnOff
- **小度音箱**: TurnOnRequest, TurnOffRequest
- **小爱同学**: turn-on, turn-off（小写+连字符）

### 3. 设备类型
- **天猫精灵**: robot_cleaner
- **小度音箱**: ROBOT_CLEANER
- **小爱同学**: vacuum-cleaner

### 4. 响应格式
- **天猫精灵/小度**: 复杂的 header + payload
- **小爱同学**: 简单的 code + message + data

## 🚀 快速迁移方案

### 代码复用率

| 层级 | 复用率 | 说明 |
|------|--------|------|
| OAuth 层 | 100% | 完全复用 |
| Repository 层 | 100% | 完全复用 |
| Service 层 | 100% | 完全复用 |
| Controller 层 | 0% | 需要新建 |
| DTO 层 | 0% | 需要新建 |

### 开发工作量

- 新增 Java 文件: 3 个
- 新增代码行数: ~250 行
- 开发时间: 4-6 小时
- 测试时间: 2 小时

## 📝 开发检查清单

- [ ] 创建 MiAIRequest.java
- [ ] 创建 MiAIResponse.java
- [ ] 创建 MiAIController.java
- [ ] 在数据库中添加小爱客户端
- [ ] 配置小米 IoT 开放平台
- [ ] 测试 OAuth 授权流程
- [ ] 测试设备发现
- [ ] 测试设备控制
- [ ] 在小爱 App 中测试语音控制

## 🎯 总结

小爱同学的对接方案特点：

1. **请求格式最简洁**: 相比天猫精灵和小度，小爱的请求格式最简单
2. **易于理解**: 使用直观的命名方式（turn-on, turn-off）
3. **快速开发**: 基于现有项目，4-6 小时即可完成
4. **高度复用**: 80% 以上代码可复用

通过统一的架构设计，现在可以用一套后端同时支持：
- ✅ 天猫精灵
- ✅ 小度音箱
- ✅ 小爱同学

实现"一次开发，多平台部署"的目标！
